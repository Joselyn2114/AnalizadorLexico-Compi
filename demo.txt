int main() {
    int x = 5, y = 10;
    if (x < y) {
        write(x + y);
    } else {
        read(x);
    }
    // Error léxico
    return 0;
}
@ // Carácter no reconocido
/* Comentario sin cerrar
